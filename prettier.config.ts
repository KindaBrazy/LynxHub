import {type Config} from 'prettier';

const config: Config = {
  singleQuote: true,
  printWidth: 120,
  bracketSpacing: false,
  bracketSameLine: true,
  arrowParens: 'avoid',
  endOfLine: 'auto',
};

export default config;
